<%= form_for(@response_domain_code) do |f| %>
  <% if @response_domain_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@response_domain_code.errors.count, "error") %> prohibited this response_domain_code from being saved:</h2>

      <ul>
      <% @response_domain_code.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code_scheme %><br />
    <%#= f.text_field :code_scheme_id %>
    <%= f.collection_select :code_scheme_id, CodeScheme.find(:all), :id, :label_with_id, :prompt => 'select a scheme' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
